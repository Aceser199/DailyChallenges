namespace DailyChallenges
{
    public class ChallengeDay2
    {
        // Given two arrays that are sorted, merge them into a single array in sorted order.

        // Example: MergedSortedArray([0, 3, 4, 31],  [4, 6, 30])
        // Output: [0, 3, 4, 4, 6, 30, 31]

        public int[] MergedSortedArray(int[] firstArray, int[] secondArray)
        {
            throw new NotImplementedException();
        }
    }
}
